# URL of the sound-scout-ai Flask microservice (used by the "Ask AI to
# Estimate" scale-calibration helper). Defaults to the service's local dev
# port if unset.
VITE_AI_SERVICE_URL=http://localhost:8000
